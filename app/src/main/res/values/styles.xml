<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 기존 SpinnerTimePickerDialog -->
    <style name="SpinnerTimePickerDialog" parent="android:Theme.Material.Light.Dialog.Alert">
        <item name="android:timePickerMode">spinner</item>
    </style>

    <!-- 잠금화면 오버레이용 투명 테마 -->
    <!-- parent 를 Material3 대신 MaterialComponents 로 변경 -->
    <style name="Theme.TransparentOverlay" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>

